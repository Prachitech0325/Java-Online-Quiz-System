# Java-Online-Quiz-System
Java Quiz Platform is a dynamic web app built using JSP, Servlets, and MySQL. It offers user login, admin question management, timed quizzes, and automatic scoring. The project follows MVC architecture, uses JDBC for database operations, and runs on Apache Tomcat.
